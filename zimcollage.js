
 $("#firstboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#firstboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("firstboxsrc").src = newlink;
        
 document.getElementById("firstboxhref").href = 
   newlink;
        index++;    
       
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#firstboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#firstboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#firstboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

///SECONDBOX//



 $("#secondboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#secondboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("secondboxsrc").src = newlink;
        
 document.getElementById("secondboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#secondboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#secondboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#secondboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})


///THIRDBOX//



 $("#thirdboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#thirdboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("thirdboxsrc").src = newlink;
        
 document.getElementById("thirdboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#thirdboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#thirdboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#thirdboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

///FOURTHBOX//



 $("#fourthboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#fourthboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("fourthboxsrc").src = newlink;
        
 document.getElementById("fourthboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#fourthboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#fourthboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#fourthboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//FIFTHBOX//


 $("#fifthboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#fifthboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("fifthboxsrc").src = newlink;
        
 document.getElementById("fifthboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#fifthboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#fifthboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#fifthboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//SIXTHBOX//


 $("#sixthboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#sixthboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("sixthboxsrc").src = newlink;
        
 document.getElementById("sixthboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#sixthboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#sixthboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#sixthboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//SEVENTHBOX//


 $("#seventhboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#seventhboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("seventhboxsrc").src = newlink;
        
 document.getElementById("seventhboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#seventhboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#seventhboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#seventhboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})


//EIGHTBOX//



 $("#eightboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#eightboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("eightboxsrc").src = newlink;
        
 document.getElementById("eightboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#eightboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#eightboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#eightboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})


//NINEBOX//


 $("#nineboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#nineboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("nineboxsrc").src = newlink;
        
 document.getElementById("nineboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#nineboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#nineboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#nineboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})


//TENBOX//


 $("#tenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#tenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("tenboxsrc").src = newlink;
        
 document.getElementById("tenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#tenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#tenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#tenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})


//ELEVENBOX//


 $("#elevenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#elevenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("elevenboxsrc").src = newlink;
        
 document.getElementById("elevenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#elevenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#elevenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#elevenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//TWELVEBOX//


 $("#twelveboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#twelveboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("twelveboxsrc").src = newlink;
        
 document.getElementById("twelveboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#thirdboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#twelveboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#twelveboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//THIRTEENBOX//


 $("#thirteenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#thirteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("thirteenboxsrc").src = newlink;
        
 document.getElementById("thirteenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#thirteenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#thirteenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#thirteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//FOURTEENBOX//


 $("#fourteenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#fourteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("fourteenboxsrc").src = newlink;
        
 document.getElementById("fourteenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#fourteenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#fourteenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#fourteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//FIFTEENBOX//


 $("#fifteenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#fifteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("fifteenboxsrc").src = newlink;
        
 document.getElementById("fifteenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#fifteenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#fifteenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#fifteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//SIXTEENBOX//


 $("#sixteenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#sixteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("sixteenboxsrc").src = newlink;
        
 document.getElementById("sixteenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#sixteenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#sixteenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#sixteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//SEVENTEENBOX//


 $("#seventeenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#seventeenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("seventeenboxsrc").src = newlink;
        
 document.getElementById("seventeenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#seventeenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#seventeenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#seventeenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})

//EIGHTEENBOX//


 $("#eighteenboxsrc").delay(500).animate({"opacity": "1"}, 700);

 setTimeout(
    function() {
     $("#eighteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);


 var imageSources = ["https://source.unsplash.com/user/erondu?"]

      var index = 0;
      setInterval(function(){
        if (index === imageSources.length) {
          index = 0;
        }
        
        var math = Math.floor(Math.random() * 10);
        var newlink = 
imageSources[index]+math;            
         document.getElementById("eighteenboxsrc").src = newlink;
        
 document.getElementById("eighteenboxhref").href = 
   newlink;
        index++;    
        
      }, 4000);


 //$("#pirst").delay(5000).animate({"opacity": "0"}, 700);

// ensures this works for some older browsers
MutationObserver = window.MutationObserver || window.WebKitMutationObserver || window.MozMutationObserver;

// the element you want to observe. change the selector to fit your use case
var img = document.querySelector('#eighteenboxsrc')

new MutationObserver(function onSrcChange(){
  // src attribute just changed!!! put code here
 $("#eighteenboxsrc").delay(500).animate({"opacity": "1"}, 700);
  
  setTimeout(
    function() {
     $("#eighteenboxsrc").delay().animate({"opacity": "0"}, 700);

    }, 3000);

})
  .observe(img,{attributes:true,attributeFilter:["src"]})





